# 0.2.7 本地候选与发布前验证记录

2026-09-21 构建来源：`6e49fd5eb1ee98811a29557fa0b2e96dabe08436`，功能分支 `feat/rc003-hid-usage-filter`。这是本地测试候选，未创建 Tag、PR 或 Release，也未 push。

## 本轮结果

- 修复语音流代次误用造成的三键增强重绑；同锁发布真正的连接代次。
- 修复断连状态在慢清理之后才可见的窗口，失效先于可能阻塞的外部调用。
- 保存、导入和重置按键统一写盘与热加载事务，避免并发时运行配置与磁盘不一致。
- 六步本地 preflight passed：前端 163，Rust 286，原有 ignored 7；随后失效发布的增量 RC003 定向 19 passed。新增测试已记录各自覆盖边界，不据此升级为硬件通过。
- 最终本地构建、生产仿真隔离检查、96 文件 Helper 完整性、0.2.6→0.2.7 安装、实际文件匹配、启动与原配置保留 passed。启动时 BLE 与音频 ready、语音 idle；增强 stopped，没有发起无人操作的提权请求。
- 安装版 4 对并发保存 IPC passed，测试后精确恢复原映射。对应证据见 [本地候选记录](evidence/local-v027-candidate-20260921.json)。

安装器：`target/release/bundle/nsis/Remote Coding_0.2.7_x64-setup.exe`，SHA-256：`6e67ec6f3b669635078f338cd924ae6eb6a04599353747a6b424c189cfef960b`。

## 停止原因与待验收项

用户要求先修 Bug、最后复查，无问题再推送；用户休息期间不提问，不能独立解决的环节停下。本轮在审查中发现并修改了实体输入生命周期，必须重新验证。当前原始任务消息也没有另一条“新 Bug”的具体内容，不能宣称那条未知反馈已修复。

下一轮需要实体操作完成：增强开启并初始化；同一 BLE 连接连续语音，确认连接与 Helper 代次不随语音变化；语音之后返回及音量仍响应；长按中断连时停止重复；真实闲置后的首次使用。不得用先前 `b67a397` 安装版的通过记录替代本候选。RC001、睡眠与完整 Codex 操作等其余边界继续按 TODO 待验收。

当时推送、远端 CI、Tag 和公开 Release 保持暂停，没有向用户提出新问题。

## 后续明确授权

维护者随后明确授权 computer use 辅助测试，并要求本次必须 push 和发布 Release，以便在另一台电脑试用。因此恢复 PR、CI 与 Preview 发布流程，上述尚未完成的硬件矩阵如实保留为 deferred，不再作为此次 Preview 的停止条件。新的“确认键长按 Ctrl+Enter 不生效”反馈另见 [Bug 记录](../Bugs/2026-09-21-enter-shortcut-identity.md)；不把旧候选验证视为该修复的验证。

## 宣传片

`target/promo/SayAll-Windows-Promo-1080p.mp4`：64 秒 1080p / 24 fps，完整 1,536 帧解码 passed，AAC 原创配乐峰值 -11.1 dB，无削波。逐场景检查通过，全片标明交互动效示意；包含本项目、Mac 原版、Windows 上游、GPL 与用户指定署名。渲染源码、字幕及可复制发布文案见 [宣传材料](../marketing/promo/README.md)。仅本地生成，未上传平台。
