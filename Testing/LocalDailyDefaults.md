# 日常默认方案与音量配置验证（2026-09-18）

## 范围

- 移除音量±六个编辑格的禁用，以及保存和运行时的配置剥离。RC003 事件送达仍待实测，不把可编辑等同于硬件通过。
- 日常方案覆盖十二个普通键；音量保留系统原始行为，语音热键独立配置。具体动作见 [README](../README.md)。
- 应用前写入最近备份并保留最初备份；备份失败不应用，相同方案重复应用不覆盖恢复点。

## 自动化

- 前端：15 文件、130 tests passed，包括音量编辑保存、完整默认动作表、最近和首次备份、存储失败不写入后端，以及重复应用保留恢复点。
- Rust workspace lib tests：198 passed、7 ignored，忽略项不计为通过；覆盖音量三手势规范化、JSON 往返和运行时保存保留。
- 本次未新增驱动、私有接口、音频路由变更或注入时序变更。

以上为该功能阶段的执行结果，不表示后续提交重新执行了全部检查。

## 原生程序与持久化

- 验证源码提交：`e42d55557cc6953d2503ac361affaf11cb39fe64`。Windows release 构建成功，独立程序启动并显示遥控器已连接。
- 原生无障碍树显示音量±六个配置格均可用；点击音量＋单击格成功打开快捷键编辑区。
- 应用日常默认方案后，界面显示保存成功与新的最近备份，最初备份恢复入口仍保留。
- 读取程序自身按键配置：映射已启用，十二键的单击、双击和长按与方案一致；返回为普通退格、双击关闭，音量无自定义动作。
- 比较应用前后的语音配置，内容保持一致；应用前的按键配置保留为恢复备份。

## 边界

RC003 返回、音量的真实输入事件，以及全部快捷键在 Codex 中的实体遥控操作仍为 deferred。界面与持久化通过不等同于实体按键验收。快捷键影响当前前台窗口；任务切换包含打开的标签页。主页、菜单、TV 单击需要约 0.3 秒双击判定等待。
